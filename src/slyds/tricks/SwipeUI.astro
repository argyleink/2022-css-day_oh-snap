---
import Prism from '/src/utils/Prism.astro'
---

<slyd id="swipe-ui">
  <h2>Swipe UI</h2>
  <demo-stage>
    <Prism lang='css' code={`
      
    `}/>
    <slyd-scrollport class="horizontal snap-x">
      {[...Array(10)].map(() => ( 
        <div></div> 
      ))}
    </slyd-scrollport>
  </demo-stage>
</slyd>
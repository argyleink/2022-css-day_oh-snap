---
import Prism from '/src/utils/Prism.astro'
---

<slyd id="pull-to-refresh">
  <h2>Pull To Refresh</h2>
  <demo-stage>
    <Prism lang='css' code={`
      
    `}/>
    <slyd-scrollport class="horizontal snap-x">
      {[...Array(10)].map(() => ( 
        <div></div> 
      ))}
    </slyd-scrollport>
  </demo-stage>
</slyd>